{
  "name": "hass-cli",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/hass-cli/src",
  "projectType": "application",
  "targets": {
    "serve": {
      "executor": "@nrwl/node:node",
      "options": {
        "buildTarget": "hass-cli:build",
        "inspect": "inspect",
        "port": 33342
      }
    },
    "prepare": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          {
            "command": "npx nx build hass-cli --configuration=scan-config"
          }
        ]
      }
    },
    "build": {
      "executor": "@nrwl/webpack:webpack",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/apps/hass-cli",
        "generatePackageJson": true,
        "fileReplacements": [
          {
            "replace": "apps/hass-cli/src/environments/environment.ts",
            "with": "apps/hass-cli/src/environments/develop.environment.ts"
          }
        ],
        "main": "apps/hass-cli/src/entrypoints/main.ts",
        "tsConfig": "apps/hass-cli/tsconfig.app.json",
        "target": "node",
        "compiler": "tsc"
      },
      "configurations": {
        "production": {
          "optimization": true,
          "extractLicenses": false,
          "inspect": false,
          "fileReplacements": []
        }
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "options": {
        "lintFilePatterns": ["apps/hass-cli/**/*.ts"]
      },
      "outputs": ["{options.outputFile}"]
    },
    "scan-config": {
      "executor": "nx:run-commands",
      "options": {
        "parallel": false,
        "commands": [
          {
            "command": "npx nx build hass-cli --configuration=production",
            "description": "Create standard production build"
          },
          {
            "command": "mkdir -p dist/configs; node dist/scan-config/hass-cli/main.js --scan-config > dist/configs/hass-cli.json",
            "description": "Scan config, output to dist"
          }
        ]
      }
    }
  },
  "tags": ["terminal"]
}
