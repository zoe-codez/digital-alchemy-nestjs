{
  "name": "pi-matrix-client",
  "sourceRoot": "libs/pi-matrix-client/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "@nrwl/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "buildableProjectDepsInPackageJsonType": "peerDependencies",
        "main": "libs/pi-matrix-client/src/index.ts",
        "outputPath": "dist/libs/pi-matrix-client",
        "packageJson": "libs/pi-matrix-client/package.json",
        "tsConfig": "libs/pi-matrix-client/tsconfig.lib.json",
        "updateBuildableProjectDepsInPackageJson": true,
        "assets": [
          "libs/pi-matrix-client/*.md",
          "libs/pi-matrix-client/package.json"
        ]
      },
      "configurations": {
        "production": {
          "optimization": true,
          "fileReplacements": []
        }
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": [
        "{options.outputFile}"
      ],
      "options": {
        "lintFilePatterns": [
          "libs/pi-matrix-client/**/*.ts"
        ]
      }
    },
    "publish": {
      "executor": "nx:run-commands",
      "options": {
        "parallel": false,
        "commands": [
          {
            "command": "npx figlet-cli -f Pagga Pi Matrix Client",
            "description": "Announce it"
          },
          {
            "command": "npx nx build pi-matrix-client --configuration=production",
            "description": "Create production optimized build"
          },
          {
            "command": "yarn publish --cwd dist/libs/pi-matrix-client --non-interactive",
            "description": "Publish it"
          }
        ]
      }
    }
  },
  "tags": ["nestjs"]
}
